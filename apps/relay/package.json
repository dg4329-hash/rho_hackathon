{
  "name": "relay",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate": "tsx src/validate.ts",
    "test": "tsx test/relay.test.ts && tsx test/files.test.ts && tsx test/keys.test.ts",
    "soak": "tsx src/soak-s1.ts"
  },
  "dependencies": { "@mesh/protocol": "workspace:*", "ws": "^8.18.0", "zod": "^3.23.8" },
  "devDependencies": { "@types/ws": "^8.5.12" }
}
