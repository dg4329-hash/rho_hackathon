{
  "name": "feed",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "mock": "tsx src/mock-relay.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "mock-daemon": "tsx src/mock-daemon.ts"
  },
  "dependencies": {
    "@mesh/protocol": "workspace:*",
    "chalk": "^5.4.1",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/ws": "^8.5.12"
  }
}
